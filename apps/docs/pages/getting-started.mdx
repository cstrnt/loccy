# Getting Started

Before you're able to use **Loccy** in your projects you will need to set-up a few things.

1. Create an account on [Loccy](https://www.loccy.app)
2. Generate an API key
3. Install the CLI tool by running `npm install -g @loccy/cli`
